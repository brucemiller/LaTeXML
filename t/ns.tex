\documentclass{article}
\begin{document}
A test of empty \foo.
Did it get namespaces?
A test of non-empty \bar{guts}.
Did it get namespaces?
What about \baz.
Did it make an empty element?
%Test mathbb $\mathbb{A}$.
\end{document}
